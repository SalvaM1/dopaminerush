El orden que hay que respetar:

Definir tamano_ventana primero
Poner el Size del nodo raíz igual a ese valor
Recién ahí acomodar el contenido adentro

Anclar todo, nunca arrastrar. Cada hijo con su ancla (Full Rect, Top Wide, Bottom Wide, Center), porque las anclas son proporciones y se adaptan solas cuando la app se estira dentro de la ventana.

Las cinco reglas del contrato:

Heredar de AppBase (extends AppBase)
Definir id_app, nombre_app, tamano_ventana, dopamina_por_interaccion
Llamar a recompensar() en la acción premiada
El nodo raíz tiene que ser un Control
Que funcione sola con F6

Los contenedores mandan. Si un nodo está dentro de un HBoxContainer o VBoxContainer, no se lo posiciona: se configura desde el contenedor o desde Container Sizing del hijo.

Mouse Filter en Ignore para todo lo decorativo, o se come los clicks que necesita el _gui_input del raíz.
